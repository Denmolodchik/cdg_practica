<h1>Likes</h1>

<ul>
    <% @users.each do |user| %>
        <li>
            <%= link_to(user.name, user_path(user)) %>
        </li>
    <%end%>
</ul>

<%= link_to 'Back', user_post_path(current_user,@post) %>